# runtime
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic[email]==2.10.4
email-validator==2.3.0
pydantic-settings==2.7.1
python-dotenv==1.0.1
python-multipart==0.0.20
anthropic==0.116.0
bcrypt==4.2.1
PyJWT==2.10.1
httpx==0.28.1
stripe==15.3.0

# Cycle 8 T-801: TOTP MFA deps.
# cryptography: Fernet for at-rest TOTP secret encryption.
# pyotp: RFC 6238 TOTP algorithm + verify.
cryptography>=42.0.0
pyotp>=2.9.0

# Cycle 7 T-701: distributed rate limiter (optional dep)
redis>=5.0.0

# Cycle 7 T-701: dev/test dep for in-process Redis simulation.
# Used by tests/test_redis_rate_limit.py for hermetic testing.
fakeredis>=2.20.0

# dev / test
pytest==8.3.4
pytest-cov==6.0.0
pytest-asyncio==0.25.0
ruff==0.8.6
mypy==1.14.0
